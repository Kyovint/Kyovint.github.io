@Library('shared-lib') _

pipeline {
    agent any
    stages {
        stage('Saludar') {
            steps {
                saludar('Mapu')  // ✅ Llama a vars/saludar.groovy
            }
        }
    }
}